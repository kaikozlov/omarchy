#!/bin/bash

if [ -f ~/.config/ghostty/config ]; then
  grep -oP '^\s*font-family\s*=\s*"\K[^"]+' ~/.config/ghostty/config | head -n1 && exit 0
fi

grep -oP 'family\s*=\s*"\K[^"]+' ~/.config/alacritty/alacritty.toml | head -n1
